# Abstain when nameplate structure is unrecognized

If the compatibility fix cannot confidently identify the exact player-name segment in a composed nameplate, it will leave that nameplate untouched rather than attempt broader recoloring. This deliberately permits Color Selection's class-icon and player-name colors to be absent because preserving True Level's character-level, Havoc-rank, and later inline formatting is the higher-priority behavior.
