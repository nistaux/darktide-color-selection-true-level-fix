# Divide nameplate visual ownership by element

When both Dependency Mods are active, Color Selection owns the class-icon and player-name colors, while True Level retains complete ownership of the character-level color, Havoc-rank color, and later inline formatting. This narrow visual contract preserves each mod's intended contribution and prevents the compatibility fix from treating the composed nameplate as a single colorable value.
